name: Bot Service — Test Core

on:
  push:
    branches: [main]
    paths:
      - 'bot-service/src/bot.ts'
      - 'bot-service/src/index.ts'
      - 'bot-service/src/types/**'
      - 'bot-service/src/interface/**'
      - 'bot-service/jest.config.js'
      - 'bot-service/tsconfig.json'
      - 'bot-service/package*.json'
  pull_request:
    branches: [main]
    paths:
      - 'bot-service/src/bot.ts'
      - 'bot-service/src/index.ts'
      - 'bot-service/src/types/**'
      - 'bot-service/src/interface/**'
      - 'bot-service/jest.config.js'
      - 'bot-service/tsconfig.json'
      - 'bot-service/package*.json'
  workflow_dispatch:

permissions:
  contents: read
  packages: read

jobs:
  test-core:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: bot-service

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: 'bot-service/package-lock.json'

      - name: Configure npm for private packages
        run: npm config set //npm.pkg.github.com/:_authToken ${{ secrets.GITHUB_TOKEN }}

      - name: Install dependencies
        run: npm ci

      - name: Run all tests
        run: npm test
