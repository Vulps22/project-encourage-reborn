name: Bot Service — Test Utils

on:
  push:
    branches: [main]
    paths:
      - 'bot-service/src/utils/**'
      - 'bot-service/package*.json'
  pull_request:
    branches: [main]
    paths:
      - 'bot-service/src/utils/**'
      - 'bot-service/package*.json'
  workflow_dispatch:

permissions:
  contents: read
  packages: read

jobs:
  test-utils:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Configure npm for private packages
        run: npm config set //npm.pkg.github.com/:_authToken ${{ secrets.GITHUB_TOKEN }}

      - name: Install dependencies
        run: npm ci

      - name: Run utils tests
        run: npm test -- src/utils
